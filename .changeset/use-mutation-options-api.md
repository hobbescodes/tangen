---
"tangen": minor
---

Use `mutationOptions` API from TanStack Query for generated mutation options instead of plain object literals. This provides better type safety and aligns with the `queryOptions` pattern.
